diff a/app/Services/Allocations/FindAssignableAllocationService.php b/app/Services/Allocations/FindAssignableAllocationService.php	(rejected hunks)
@@ -39,6 +39,7 @@ class FindAssignableAllocationService
         // server.
         /** @var Allocation|null $allocation */
         $allocation = $server->node->allocations()
+            ->lockForUpdate()
             ->where('ip', $server->allocation->ip)
             ->whereNull('server_id')
             ->inRandomOrder()
@@ -102,6 +103,7 @@ class FindAssignableAllocationService
 
         /** @var Allocation $allocation */
         $allocation = $server->node->allocations()
+            ->lockForUpdate()
             ->where('ip', $server->allocation->ip)
             ->where('port', $port)
             ->firstOrFail();
